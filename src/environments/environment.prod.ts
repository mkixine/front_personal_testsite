export const environment = {
  production: true,
  apiBaseUrl: 'https://mkixine-json.g.kuroco.app',
  apiTimeout: 30000,
  maxRetryAttempts: 1,
  retryDelay: 1000,
  enableLogging: false,
  enablePerformanceMonitoring: true
};
